import React from "react";
import { HashRouter } from "react-router-dom";

function About() {
    return(
        <span>About this page : I built because I love movies</span>
    )
}

export default About;